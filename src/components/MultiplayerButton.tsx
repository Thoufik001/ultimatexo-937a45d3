
import React from 'react';
import BotDifficultyInfo from './BotDifficultyInfo';

const MultiplayerButton: React.FC = () => {
  // This component now just renders the BotDifficultyInfo component
  return <BotDifficultyInfo />;
};

export default MultiplayerButton;
